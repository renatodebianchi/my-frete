namespace MyFrete.Migrations;

/// <summary>
/// Hosts the EF Core <c>DbContext</c> and migrations. The initial migration
/// (configuration, audit_event, idempotency_key, notification_outbox) lands in Phase 2 (T010).
/// </summary>
public static class MigrationsAssembly;
